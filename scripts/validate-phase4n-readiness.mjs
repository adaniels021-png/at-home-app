import fs from'node:fs';import assert from'node:assert/strict';import crypto from'node:crypto';const r=n=>JSON.parse(fs.readFileSync(`data/curriculum/${n}`,'utf8'));const p=r('phase4n-production-preflight-v1.json'),a=r('phase4n-activation-routing-contract-v1.json'),e=r('phase4n-entitlement-transition-qa-v1.json'),s=r('phase4n-security-validation-v1.json'),b=r('phase4n-rollback-rehearsal-v1.json'),ready=r('phase4n-activation-readiness-v1.json');assert.deepEqual(p.baseline,{lessons:299,approved:211,strictReady:198,readyFree:2,readyPro:196,domains:21,skills:54,stages:264,mappings:299,activePrimary:0,readySkills:42});assert.equal(p.hashes.phase4e,'be6fe07f503c74c05c5be0557ee249ca8da42bab392f40638d7fe1f12caf5284');assert.equal(a.phase4NFinalMode,'LEGACY');assert.equal(a.cohortPercentage,0);assert.equal(e.classification,'ENTITLEMENT_CONTROL_PASSED');assert.equal(e.proLeakage,false);assert.equal(s.passed,true);assert.equal(b.passed,true);assert.equal(b.parentVisibleThroughout,'LEGACY');assert.equal(ready.parentReceivedV2,false);assert.equal(ready.activePrimary,0);const sql=fs.readFileSync('supabase/migrations/20260813120000_create_recommendation_activation_control.sql','utf8');for(const forbidden of['lesson_library set','lesson_curriculum_mappings set','is_active_primary = true','update public.lesson_personalization_metadata'])assert(!sql.toLowerCase().includes(forbidden));assert(sql.includes("values(true,'LEGACY','phase4m-shadow-v2',false,0)"));assert.equal(crypto.createHash('sha256').update(fs.readFileSync('lib/personalization/recommendation/scoring.ts')).digest('hex'),p.hashes.phase4e);console.log(JSON.stringify({valid:true,mode:a.phase4NFinalMode,security:true,rollback:true,entitlement:e.classification,parentV2:false},null,2));
